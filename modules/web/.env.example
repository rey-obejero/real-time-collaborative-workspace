# Base URL of the Workspace API consumed by the web client.
# Copied by Vite into the client bundle at build time; keep in sync with
# .env.development (dev server) or override per environment as needed.
VITE_API_URL=https://api.workspace.localhost/api/v1
