# Design Prototypes

This directory serves as a playground for UI prototyping and iterations. Quick prototypes are built via single-file artifacts using HTML, CSS, and JS with Alpine.js and TailwindCSS imported via CDN. Available agent skills to assist in prototyping are `design-taste-frontend` and `impeccable`.
